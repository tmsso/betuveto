# Memory - 2026-04-04

## 19:44 - Pre-compaction memory flush

### Key Current Project: RTSP Camera Snapshot Capture

**Goal**: Download and use FFmpeg to capture a single snapshot from an RTSP camera stream.

**Constraints & Preferences**:
- Portable (static) FFmpeg build for compatibility
- x86_64 architecture for Debian 13 on ThinkPad X220
- RTSP stream: `rtsp://rtsp:12345678@192.168.1.112:554/av_stream/ch0`
- Output snapshot to `~/camera_snapshot.jpg`
- Use TCP transport for RTSP to ensure reliable streaming

**Progress**:

Done:
- Downloaded static FFmpeg build from johnvansickle.com
- Extracted binary to `~/tools/ffmpeg`
- Verified installation via `ffmpeg -version`

In Progress:
- Capturing snapshot from RTSP stream (currently blocked)

**Blocked**:
- The `ffmpeg` command failed because the `image2` muxer requires either an image sequence pattern (e.g., `%03d`) or the `-update` option for single-image output.

**Key Decisions**:
- Use static build for portability and no external dependencies
- Use TCP transport for RTSP to avoid UDP packet loss

**Critical Context**:
- FFmpeg binary location: `~/tools/ffmpeg`
- RTSP URL: `rtsp://rtsp:12345678@192.168.1.112:554/av_stream/ch0`
- Output file: `~/camera_snapshot.jpg`
- Error: `[image2 @ ...] The specified filename ... does not contain an image sequence pattern... Use -update option`

**Next Steps**:
1. Re-run ffmpeg with `-update 1` flag to write a single image:
   `~/tools/ffmpeg -rtsp_transport tcp -i "rtsp://rtsp:12345678@192.168.1.112:554/av_stream/ch0" -frames:v 1 -f image2 -update 1 ~/camera_snapshot.jpg`

### Other File Modifications
- Modified: `proactivity/mategye_monitor.sh` (content unknown, should be checked)
- No recent memory search available due to OpenAI embedding quota exhaustion

### System Status
- Current time: Saturday, April 4th, 2026 — 19:44 (Europe/Budapest)
- Working directory: /home/tomi/.openclaw/workspace
- Memory search unavailable: OpenAI embedding quota exceeded