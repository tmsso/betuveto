# Environment variables
.env
.env.local
.env.production
.env.preview
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Git
.git/
.gitignore

# Note: data/magyar-szavak.txt is intentionally included in the repository
# to ensure the game works without additional setup

# Logs
*.log

# Streamlit
.streamlit/
.cache/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Dependencies
node_modules/
frontend/node_modules/
*.whl
*.egg-info/

# Local config
config.toml
secrets.toml

# The `lib/` rule above targets Python build output. Our shared TypeScript game logic
# also lives in lib/ and must be committed — the API functions import it.
!lib/
!lib/**

# Openclaw / agent runtime (never commit these)
.clawhub/
.openclaw/
skills/
subagents/
memory/
proactivity/
tech-news/
AGENTS.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md

# Claude Code skills are a different tool from Openclaw — re-include despite the broad
# skills/ rule above.
!.claude/skills/
!.claude/skills/**

# Virtual environments (word-game-env)
word-game-env/

# Camera snapshots and unrelated data files
cam_*.jpg
*.pdf
*_results*.csv
zrinyi_*/
# Vercel CLI project link and the secrets it pulls. `.env*` (as the CLI writes it) would
# also swallow the committed .env.example, so be specific.
.vercel/
.env.local
